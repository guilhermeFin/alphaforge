# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Secrets & config — NEVER commit API keys
.env
.env.*
# the template below carries NO secrets and SHOULD be tracked:
!.env.example
*.key
secrets.*

# Data — do not commit vendor data or large artifacts (we serve derived results, not feeds)
data/
*.parquet
*.csv
*.feather
cache/

# Accounts data layer — the local SQLite store holds (encrypted) customer data; never commit it
*.db
*.db-wal
*.db-shm
alphaforge.db

# Marketing build artifacts (verification screenshots)
marketing/assets/_*.png

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
